# pnpm: hoist transitive deps that are imported directly by app code but only
# declared transitively (e.g. @tiptap/core, pulled in by @tiptap/react). The
# repo was originally bootstrapped with npm (flat node_modules), so several
# such direct imports rely on hoisting. This keeps `pnpm install/build` working.
shamefully-hoist=true
